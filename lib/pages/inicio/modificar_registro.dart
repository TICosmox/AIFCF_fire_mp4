import 'package:flutter/material.dart';

class ModificarRegistro extends StatelessWidget {
  const ModificarRegistro({super.key});

  @override
  Widget build(BuildContext context) {
  return const Scaffold(
   body: Text('Hola'),
  );
  }
}